/*
import type { Core } from "@strapi/strapi";

const config: Core.RouterConfig = {
  type: "content-api",
  routes: [
    {
      // Path defined with a URL parameter
      method: "GET",
      path: "/article/loadfirstlatestarticle",
      handler: "api::article.article.loadFirstArticle",
      config: {
        policies: [],
        middlewares: [],
      },
    },
  ],
};

export default config;
*/
